import {
    Checkbox
}from "../components/ui/checkbox";

# Checkbox Component

**Contributed by Nuojin Zhang**

A checkbox component allows users to make a binary choice.

## Basic Checkbox Component

### Design:
This component utilizes the Checkbox component from the UI library to create interactive checkboxes. 
Each checkbox is accompanied by a label for better accessibility and user experience.
The use of a toast notification system provides feedback to the user when a checkbox is clicked, informing them of the action they performed.

### How to behave:
- **Default Checkbox**:
      Clicking on the default checkbox triggers a toast notification with the message "Checkbox Clicked", indicating that the checkbox has been interacted with.
- **Checked State Checkbox**:
   This checkbox is initially in the checked state.
   Clicking on the checkbox also triggers a toast notification with the message "Checkbox Clicked", indicating that the checkbox has been interacted with.

### How to use Checkbox Component:
- Installation：
   Ensure that you have the necessary dependencies installed in your project. If not, install them using npm by running the following command in your terminal:
   ```
   npx shadcn-ui@latest add checkbox
   ```
- Usage:
Copy the react code above and import into your React application where you want to use it
   ```
   import { Checkbox } from "../../../../../styles/components/ui/checkbox";
   import { useToast } from "../../../../../styles/components/ui/toast/use-toast";
   import { Toaster } from "../../../../../styles/components/ui/toast/toaster";
   ```
- Rendering the Component:
Place the  `CheckboxDemo ` component within your JSX to render it on the desired page or component
- Customization:
Customize the  `CheckboxDemo ` component as needed to match your application's design and requirements. Adjust styles, event handling, and any additional functionality to suit your use case.
   - **Example:** Use this checkbox to indicate agreement with terms and conditions during user registration.



<Checkbox />

## Version
| Version | Editor | Reviewer | Date | Description |
| ---- | ---- | ---- | ---- | ---- |
| v1.0 | Nuojin Zhang | Nuojin Zhang | 10 Apr 2024 | React Version |
| v2.0 | Nuojin Zhang | Nuojin Zhang | 25 Apr 2024 | Documentation |
| v3.0 | Yingjian Di | Yingjian Di | 25 Apr 2024 | Peer Review |

